import { templateRoot, cardElement } from './util'  


module.exports = function(db, res) {

    // db.loadDatabase({}, ()=> {
    //     const events = db.getCollection('events')
    //     console.log(events.data);
    //     const cards = []
    //     events.data.forEach(el => cards.push(cardElement(el.image, el.title, 'my sub')))
    //     res.status(200).send(templateRoot('generic', cards))
    //   })



	// res.status(200).send(JSON.stringify(inicio));

   
    

}